#pragma once
#include <vector>
#include <functional>

struct Transition {
	struct next
};

struct AnimState {
	float completionPercent = 0;
	float length = 1;
	std::vector<Transition>

	AnimState() {

	}
};